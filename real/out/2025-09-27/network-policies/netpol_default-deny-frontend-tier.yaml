apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-frontend-tier
spec:
  podSelector:
    matchLabels:
      subnet: frontend-tier
  policyTypes:
  - Ingress
  - Egress
